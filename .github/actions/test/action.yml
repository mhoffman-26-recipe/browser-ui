name: Run Tests
description: Runs the test suite after setup is complete

runs:
  using: "composite"
  steps:
    - name: Run tests
      env:
        GITHUB_TOKEN: ${{ github.token }}
      run: npm run test
      shell: bash